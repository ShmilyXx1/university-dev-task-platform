<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.svtu.mapper.AdminMenuMapper">
    <select id="selectUserAllMenu" resultType="java.lang.String">
        SELECT DISTINCT t_menu.menu_name
        from t_user_role
                 inner join t_role on t_role.role_id=t_user_role.role_id
                 inner join t_menu_role on t_user_role.role_id=t_menu_role.role_id
                 inner join t_menu on t_menu_role.menu_id=t_menu.menu_id
        where t_user_role.user_id=#{userId}
    </select>
</mapper>