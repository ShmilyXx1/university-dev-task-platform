<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.svtu.mapper.MenuRoleMapper">

    <select id="selectMenRole" resultType="java.lang.String">
        select t_menu.menu_account
        from t_menu_role
        inner join t_menu on t_menu_role.menu_id=t_menu.menu_id
        where t_menu_role.role_id=#{roleId}
    </select>
</mapper>