<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.svtu.mapper.UserRoleMapper">

    <select id="selectUserRoleName" resultType="java.lang.String">
        select t_role.role_name
        from t_user_role
        inner join t_role on t_user_role.role_id=t_role.role_id
        where t_user_role.user_id=#{userId} and t_user_role.role_id !=4
    </select>
</mapper>